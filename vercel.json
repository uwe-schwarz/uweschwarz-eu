{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.x",
  "installCommand": "npm exec --yes --package=$(node -p 'require(\"./package.json\").packageManager') -- bun install --frozen-lockfile"
}
